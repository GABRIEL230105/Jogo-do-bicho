name: CI

on:
  push:
  pull_request:

jobs:
  teste:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Backend
        run: |
          cd backend
          yarn
          yarn test
        env:
          NODE_ENV: test
          JWT_SECRET: abc123

      - name: Frontend
        run: |
          cd frontend
          yarn
          yarn build